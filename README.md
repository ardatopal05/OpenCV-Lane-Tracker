# OpenCV-Lane-Tracker

A simple lane-tracking project written in Python.

This project detects lane markings from the captured image (Live for Speed window) and generates steering input to keep the vehicle centered.

Live for Speed (LFS) was used only as a simulation environment during development to provide real-time road images. This project is not an LFS modification or plugin; the simulator was simply used for testing the lane-tracking algorithm.
